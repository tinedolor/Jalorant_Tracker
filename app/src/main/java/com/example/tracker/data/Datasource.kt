package com.example.tracker.data

import com.example.tracker.R
import com.example.tracker.model.card
/*
class Datasource() {
    fun loadcard(): List<card> {
        return listOf<card>(
            card(R.drawable.match1)
        )
    }
}
*/